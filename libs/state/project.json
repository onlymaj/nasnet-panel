{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "state",
  "root": "libs/state",
  "sourceRoot": "libs/state",
  "projectType": "library",
  "tags": ["type:lib", "scope:state"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/state/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rg --files libs/state -g 'tsconfig.lib.json' -g 'tsconfig.spec.json' -g 'tsconfig.storybook.json' -g '.storybook/tsconfig.json' | sort | xargs -I{} -P 4 tsc --noEmit --project \"{}\""
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rg --files libs/state -g 'vitest.config.ts' | sort | xargs -I{} sh -c 'dir=$(dirname \"$1\"); cd \"$dir\" && vitest run --config vitest.config.ts --passWithNoTests' _ {}"
      }
    }
  }
}
